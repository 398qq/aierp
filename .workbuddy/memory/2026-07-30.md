# 2026-07-30 工作日志

## 架构梳理
- 通读 CLAUDE.md / AGENTS.md / docs/architecture/001-design-audit / DESIGN.md 及实际目录结构（backend/app、frontend/src）
- 用两张架构图（系统全景 + 后端 DDD 分层）建立整体认知，已 inline 呈现给用户
- 关键结论：后端两套分层并存（遗留 services ~90% + 新 DDD 少量用例），v6 阶段 1-3 重构已落地
- 长期项目笔记已写入 MEMORY.md

## 四层架构最优选方案建议
- 针对前后端协议、后端分层、缓存、前端四个层面给出推荐方案，矩阵图已 inline 呈现
- 结论：P0 前后端协议走 OpenAPI 生成 TS client；P1 后端分层渐进 use case 迁移 + 缓存补 Redis Pub/Sub 跨 worker 失效；P2 前端框架迁移暂缓，先补内部质量
- 关键约束：优先迁移成本可控、收益明确的方案，避免为迁而迁

## 待办（明天继续）
- 用户决定明天再细化执行计划
- 明天优先定 P0「OpenAPI 生成 TS client」的执行步骤（看 config/config.ts、src/api/client.ts、FastAPI openapi 输出）
- 再排 P1：后端 use case 迁移 PoC + 缓存 Redis Pub/Sub 跨 worker 失效
- P2 前端框架迁移暂缓
