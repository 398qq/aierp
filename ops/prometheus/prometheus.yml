global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aierp-backend
    metrics_path: /metrics/prometheus
    static_configs:
      - targets: ["localhost:8080"]
        labels:
          service: aierp
          tier: backend
