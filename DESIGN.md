# AIERP Enterprise Operations Design System

本文件是 AIERP 前端 UI 的唯一设计基线。目标是让客户、销售、采购、库存、财务、报表与系统治理页面形成统一、紧凑、可扫描的企业运营体验。

## 设计原则

1. 业务优先：页面围绕状态、金额、数量、责任人、时限和下一步动作组织，不采用营销落地页式排版。
2. 高信息密度：桌面端优先使用紧凑表格、过滤器、批量操作和侧边详情；减少无意义留白。
3. 一致可预测：同一种动作、状态和层级在所有业务域使用同一视觉与交互。
4. 风险可见：异常、逾期、库存不足和审批阻塞必须清楚，但避免大面积高饱和颜色。
5. 移动端可操作：核心流程允许查询、查看详情、审批和执行关键动作；复杂表格可横向滚动。

## 色彩

| 语义 | 色值 | 用途 |
| --- | --- | --- |
| Primary | `#1d4ed8` | 主按钮、链接、焦点边框、选中态 |
| Primary hover | `#2563eb` | 悬停与辅助强调 |
| Primary deep | `#17365d` | 表单分组标题、单据抬头 |
| Primary press | `#1e40af` | 按下态 |
| Primary background | `#edf3fa` | 表单分组、选中行、弱提示背景 |
| Sider navy | `#10233f` | 主导航背景 |
| Text | `#172033` | 标题和正文 |
| Secondary text | `#667085` | 辅助信息 |
| Workspace | `#f5f7fa` | 应用工作区 |
| Border | `#d8dee8` | 卡片、表格和输入框边界 |
| Success | `#15803d` | 完成、到账、正常 |
| Warning | `#d97706` | 待处理、临期、低库存 |
| Danger | `#dc2626` | 失败、逾期、缺货、拒绝 |

颜色用于表达语义，不使用装饰性紫色、彩虹渐变或任意新增品牌色。状态优先使用浅背景标签，危险色只覆盖需要注意的局部。

## 应用框架

- 左侧导航：桌面宽 `224px`，折叠宽 `64px`，深海军蓝背景。
- 导航按业务域分组：经营总览、客户与销售、采购与供应链、产品与库存、财务管理、报表与分析、协作与审批、系统治理。
- 顶栏：面包屑、全局功能搜索、AI、帮助、全屏、通知和当前用户。
- 工作区：`#f4f7fb` 背景，桌面内边距 `20px`。
- 移动端：抽屉式全量导航，底部固定总览、客户、订单、通知、更多五个入口。

## 页面模板

### 台账/列表页

从上到下依次为：页面标题与主动作、指标带、过滤/搜索工具栏、批量操作、数据表格、分页。表格行高目标为 `40px`，数字使用等宽数字特性并右对齐。

### 工作台

顶部展示少量关键指标；主体突出待办、异常、趋势和最近业务活动。图表必须回答明确业务问题，不能只作装饰。

### 详情页

标题区展示单据号/对象名、状态和主操作；下方使用概要、明细行、关联单据、操作记录与审计信息。常用动作保持可见，破坏性动作放入更多菜单并二次确认。

### 编辑/创建页

按业务语义分组字段。必填、校验、异步状态和提交结果必须清楚。单据金额和行项目汇总保持可见。

- 表单控件默认使用 `#fafbfc` 背景和 `#d8dee8` 边框，聚焦后切换为白底、`#1d4ed8` 边框与低透明度焦点环。
- 字段标签使用 `#344054`，辅助说明使用 `#667085`，禁用内容使用 `#98a2b3`。
- 表单分组标题使用 `#edf3fa` 背景和 `#17365d` 文字，保持业务层级清晰。
- 校验错误使用 `#dc2626`，警告使用 `#d97706`；颜色必须与文字说明同时出现。
- 主提交动作使用深海蓝，每个操作区只保留一个主按钮；删除、作废不作为常驻实心主按钮。

## 基础组件规范

- 页面标题：`20px/700`，副标题 `12px`。
- 正文与表格：`12–14px`，使用系统字体、苹方或微软雅黑。
- 卡片：白底、`1px #d8dee8` 边框、`8px` 圆角、轻阴影。
- 按钮：`6px` 圆角；每个操作区最多一个主按钮。
- 输入框：`6px` 圆角，聚焦使用主蓝色边框。
- 标签：`4px` 圆角，颜色只表示确定的业务状态。
- 表格头：`#f7f9fc` 背景，中性深灰文字；悬停行使用 `#eff6ff`。
- KPI：值大于标签，数值使用 tabular numbers；趋势色必须与好坏方向一致。

## 响应式规则

- `>= 1200px`：完整侧栏，多列指标与筛选器。
- `768–1199px`：允许侧栏折叠，内容列数减少。
- `< 768px`：隐藏桌面侧栏，启用抽屉和底部导航；标题动作纵向排列；指标两列；表格横向滚动；抽屉宽度不超过视口。

## 可访问性与反馈

- 图标按钮必须有 `aria-label` 或 Tooltip。
- 不只依赖颜色表达状态，必须同时显示文本或图标。
- 加载、空数据、失败和重试状态不得缺失。
- 键盘焦点清晰；点击目标在移动端不小于约 `40px`。
- 批量、删除、审批和状态转换必须给出明确反馈。

## 实现入口

- 主题与尺寸变量：`frontend/src/design-tokens.ts`
- 应用框架：`frontend/src/layouts/MainLayout.tsx`
- 框架样式：`frontend/src/styles/app-shell.css`
- 运营页面通用样式：`frontend/src/styles/operational-ui.css`
- 全局 ERP 表单样式：`frontend/src/styles/erp-form.css`
- 通用模板：`frontend/src/ui/PageHeader.tsx`、`MetricBand.tsx`、`ModuleShell.tsx`

新增页面应优先复用这些入口，不在页面内创建独立的品牌色、字体、圆角或导航体系。
